{
  "name": "platform-solo-sutra",
  "version": "1.0.1",
  "description": "六祖坛经·阅藏 — 经折装数字阅读器",
  "private": true,
  "scripts": {
    "parse": "node scripts/parse_cbeta.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "android:sync": "npx cap copy android",
    "android:build": "npm run android:sync && cd android && .\\gradlew assembleDebug",
    "release": "npm run build && npm run android:build && node -e \"const v=require('./package.json').version;require('fs').cpSync('android/app/build/outputs/apk/debug/app-debug.apk','release/platform-solo-sutra-v'+v+'.apk');console.log('✅ release/platform-solo-sutra-v'+v+'.apk')\""
  },
  "devDependencies": {
    "fast-xml-parser": "^4.5.0",
    "pdf-parse": "^2.4.5",
    "pdfjs-dist": "^4.8.69",
    "pinyin-pro": "^3.28.1",
    "serve": "^14.2.0",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "lxgw-wenkai-webfont": "^1.7.0"
  }
}
